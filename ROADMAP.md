# MangaCrisp Roadmap

この文書は、公開後の機能追加と修正をユーザー価値単位で管理します。実装中の詳細はGitHub IssueとDraft Pull Requestで追跡します。

## P0: 画質設定を分かりやすくする

追跡: [Issue #2 原画＋3種類の簡単画質モード](https://github.com/jydie5/MangaCrisp/issues/2)

状態: 実装済み（PR #5でmainへ反映）

通常の読書では「原画」と3種類の補正モードだけを表示します。名称は目視評価後に確定します。

| モード案 | 初期設定 | 目的 |
|---|---|---|
| 原画 | 補正なし | 元画像をそのまま表示・比較 |
| 自然 | Real-CUGAN se / noise 0 | 線とトーンを自然に整える標準設定 |
| クリーニング | Real-CUGAN se / noise 3 | 古いスキャンや圧縮荒れを強めに抑える |
| 高画質 | Real-CUGAN pro / noise 3 | 処理時間より仕上がりを優先 |

旧「軽量」は「標準補正」と同じ処理設定だったため廃止しました。

### 完了条件

- 初めての利用者がモデル名、noise、tileを理解しなくても画質を選べる
- 選択中のモードと原画比較状態が読書画面から判別できる
- 既存の補正結果と見開き単位の同時切り替えを維持する

## P1: マニュアルモードと設定保存

追跡: [Issue #3 マニュアル画質モードとカスタム設定保存](https://github.com/jydie5/MangaCrisp/issues/3)

状態: 実装済み（PR #5でmainへ反映）

詳細設定を必要とする利用者だけがマニュアルモードへ切り替えられるようにします。

- モデル、倍率、noise、tile、TTA、補正スキップ解像度を編集
- 名前を付けてカスタム設定を保存、呼び出し、上書き、削除
- 利用できないモデルとパラメータの組み合わせを保存前に検証
- 設定はユーザー領域へ保存し、アプリ更新後も維持
- 簡単モードへ戻してもカスタム設定を消さない

## P1: 気づかせない自動先読み

追跡: [Issue #4 読書速度に追従するバックグラウンド先読み](https://github.com/jydie5/MangaCrisp/issues/4)

状態: 実装済み（PR #5でmainへ反映）

読書操作を優先し、通常は先読み処理の存在を意識させない設計にします。

- 表示中の見開き、直後の見開き、遠い前方、後方の順に処理
- 基本値は前方12ページ、後方4ページ
- ページ送り間隔と実測補正時間から前方12〜24ページを自動調整
- 方向転換時は実行中の処理を破棄せず、未着手の対象外ページだけを飛ばして現在位置へ追従
- 原画は即時表示し、左右の補正がそろった時点で見開き単位に切り替え
- PNG読込、グレースケール変換、画面サイズへの縮小は表示スレッド外で実行
- 読書位置は連続操作をまとめ、操作停止後または終了時に保存
- CPU/GPU負荷、メモリ、ディスク使用量に上限を設ける
- 通常時は詳細な処理メッセージを隠し、エラー時だけ簡潔に通知

## P2: 性能評価

- M1、M4 Proで同じ入力を使い、1ページ処理時間と連続ページ送りを測定
- A18 Pro搭載MacBook Neo（8GB）で起動、AI比較、先読み、メモリ負荷を検証
- 代表解像度ごとに原画、se、pro、TTAを比較
- キャッシュに追いついた場合の待ち時間と原画表示へのフォールバックを測定
- 10分以上の連続読書でメモリとキャッシュ容量が上限内に収まることを確認

## P1: PDF対応

状態: v0.7.0-betaで実装済み

スキャン文書で一般的なPDFを本棚へ登録し、全ページを事前変換せず現在位置から
オンデマンド描画します。表紙だけは取り込み時に生成し、原画を先に表示してから
必要な場合だけ既存AI補正へ渡します。

- 暗号化されていないローカルPDF
- ラスター・ベクター・混在ページとカラー保持
- 300ページ以上でも全ページ事前描画を行わない
- 描画ページと補正ページを別の有界キャッシュで管理
- PDFレンダラーはライセンス、macOS/Windows同梱、スレッド安全性を技術検証して確定

## P1: Comic EPUB対応

状態: 要件定義・設計完了、PDF共通基盤の後に実装

DRMフリーEPUB 2/3のうち、画像ページ列へ欠落なく変換できる固定レイアウト漫画を
対象とします。OPFのspine順、RTL/LTR、見開き位置、表紙を尊重します。

- リフロー型、DRM、スクリプト必須、外部リソース必須は初期対象外
- ZIP内ファイル名ではなくOPF spineをページ順に使用
- path traversal、zip bomb、XML外部エンティティを防止
- 非対応EPUBと破損EPUBを区別して案内

## P2: OPDS 1.2ネットワーク書庫

状態: 要件定義・設計完了、PDF・Comic EPUBの後に実装

Komga、Kavita、Calibre Content Serverを本棚から閲覧し、選択した本を管理領域へ
ダウンロードして既存のローカル読書・AI補正へ渡します。初期版はストリーミングせず、
ダウンロード後のオフライン読書を優先します。

- Navigation/Acquisition Feed、検索、ページ分割
- CBZ/CBR/PDF/EPUB取得
- Basic/Digest認証
- macOS Keychain／Windows Credential Managerへの秘密保存
- 非同期閲覧・ダウンロード・キャンセル
- OPDS 2.0、独自API、読書位置同期は後続評価

詳細要件と共通設計:

- [English](docs/product/publication-sources.md)
- [日本語](docs/product/publication-sources.ja.md)

次回リリースの範囲と品質ゲート:

- [English](docs/releases/v0.7.0-beta-plan.md)
- [日本語](docs/releases/v0.7.0-beta-plan.ja.md)

リリース順は `v0.7.0-beta: 共通ページ基盤 + PDF`、
`v0.8.0-beta: Comic EPUB`、`v0.9.0-beta: OPDS 1.2` とします。
PDF、EPUB、ネットワーク書庫を同じリリースへ同時投入しません。

## P1: マウス中心の読書操作

状態: 実装済み

MangaMeeyaのマウス中心の操作思想を参考にしつつ、MangaCrispの右綴じ・左綴じ設定と
進行情報オーバーレイを両立させます。

- 画面左右40%は読書方向に連動して次／前の見開きへ移動
- 中央20%はページ進行情報の表示／非表示
- `Shift`を押しながら左右領域をクリックすると1ページだけ移動
- 右クリックは読書方向にかかわらず前の見開きへ移動
- ドラッグやダブルクリックで意図せず複数回移動しない
- 設定パネル、ヘルプ、次巻ボタンのクリックをページ送りに使わない
- MacとWindowsで共通の入力判定とテストを利用

M4 Pro実測（15〜19MBの補正版PNG、1600×1000ウィンドウ）:

- キー入力処理: 0.68ms
- キャッシュ未命中時の原画見開き表示: 15.57ms
- バックグラウンドでの補正版見開き完成: 181.64ms
- 補正版予熱済みのページ送り: 0.57ms

## P1: 連番スクリーンキャプチャ初版

状態: v0.7.1-betaでmacOS初版を実装済み。2026-08-13に実機ヒューマンチェック合格

権利または許諾を持つ画面素材を、利用者の手動操作で連番PNGへ保存し、レビュー後に
CBZ/ZIPへまとめます。特定サービス専用取得、自動ページ送り、無人撮影、DRMや
キャプチャ拒否の回避は行いません。

- 本棚から独立した小型Captureコントローラを開く
- 初版は固定矩形範囲を必須とし、macOS Apple Siliconから実装
- 変更可能なグローバルショートカットで1入力につき1枚を保存
- 6桁採番、原子的保存、直前取消、同番号撮り直し、未完了セッション復旧
- 完全一致・近似重複、黒・透明・極小画像をレビュー時に警告
- 並べ替え、削除、回転後にCBZ/ZIPを生成し、任意で本棚へ登録
- 元PNGは既定で保持し、利用者が明示した場合だけ削除
- Retina、複数画面、権限拒否、フルスクリーン、配布アプリで実機検証

初回ヒューマンチェックは、自作または再配布可能素材を10ページ手動撮影し、取消、
重複警告、CBZ出力、MangaCrispでの順序確認までを対象とします。

詳細:

- [日本語](docs/product/sequential-screen-capture-v1.ja.md)
- [English](docs/product/sequential-screen-capture-v1.md)

初版は`v0.7.1-beta`へ収録します。既定の`v0.8.0-beta: Comic EPUB`、
`v0.9.0-beta: OPDS 1.2`の順序は変更しません。今後の画面単位取得、見開き分割、
JPEG出力はキャプチャ初版とは分離して判断します。

## P1: 読書・本棚UXメンテナンス

状態: 実装予定。キャプチャ初版とは別の `core/reader-bookshelf-ux` で扱う

キャプチャ機能と混ぜず、既存利用者へ直接効く小さな更新として実装します。

### 初回見開きの補正優先

現在は原画を先に表示し、通常の先読みキューが動いた後に補正済み見開きへ切り替わるため、
最初に開いた位置だけ数秒待つ場合があります。

- 補正キャッシュがあれば初回描画から補正版を使う
- キャッシュがなければ原画を100ms以内の目標で表示し、表示中ページを最優先で補正する
- 表示中見開きを前方12〜24ページの先読みより先に処理する
- 左右の補正がそろってから見開き単位で切り替え、片側だけ補正済みにしない
- 補正完了時間そのものはモデルとGPUに依存するが、キュー待ち時間を発生させない
- 初回補正中にページ移動した場合は、古い結果で現在表示を上書きしない

### マウスで本棚へ戻る

- 中央クリックで表示する透過読書情報へ、本棚アイコンボタンを追加する
- ボタンは全画面でも利用でき、押すと読書位置を保存して同じウィンドウの本棚へ戻る
- ボタン操作をページ送りとして扱わない
- 設定パネル内の既存「本棚へ戻る」と `Esc` の動作は維持する

### 本棚の右クリックと複数削除

- 本棚を複数選択可能にし、macOSはCommand／Shift、WindowsはCtrl／Shiftへ従う
- 本の右クリックメニューへ「読む」「本棚から削除」を追加する
- 未選択の本を右クリックした場合はその本だけを選択し、選択済み集合上では複数選択を保つ
- 削除確認は1回だけ表示し、対象冊数と先頭数件の書名を示す
- MangaCrisp管理コピー、読書位置、しおり、対応キャッシュを削除し、元ZIP/RAR/7z/PDFは残す
- 一部削除に失敗した場合は成功分を反映し、失敗した本をまとめて通知する
- Delete/Backspaceキーと下部の削除ボタンも複数選択へ対応する

### 全画面のマウスカーソル自動非表示

- 全画面の画像領域で2秒操作がなければカーソルを非表示にする
- マウス移動、クリック、キー操作で直ちに表示し、タイマーを再開する
- 読書情報、次巻ボタン、設定パネル、メニュー、ダイアログ上では隠さない
- 全画面解除、フォーカス喪失、アプリ終了時は必ず通常カーソルへ戻す
- ドラッグ中やモーダル表示中にカーソルを隠さない

### 完了条件

- 新規に本を開いた時、表示ページの補正が先読み待ちにならない
- マウスだけで読書画面から本棚へ戻れる
- 1冊と複数冊の右クリック／キー削除が同じ確認・削除規則を使う
- 全画面で静止時だけカーソルが消え、操作再開時に必ず戻る
- macOSとWindowsで共通テストを使い、OS固有キー表記だけを分離する

## P1: Windowsポータブル版

状態: Development Preview公開中（正式版の外部実機証跡は未完了）

Windows 11 x64で、共通コードのプラットフォーム分離、7-Zipを使ったRAR/CBR対応、
PyInstaller one-folder、配布監査、展開後スモーク起動、`pyside6-deploy`との
実測比較まで完了し、初版はPyInstallerを採用します。
Real-CUGANは固定ソースをZigでビルドし、Microsoft VC/OpenMPおよびMinGWのランタイムDLLを
同梱しない経路へ切り替えました。実行ファイル、PE import、モデル、ライセンス、来歴を
配布監査で検証します。NVIDIA RTX 2070 SUPERで固定demoの自動検証は通過済みです。
正式リリース前に、同一エンジンSHA-256でIntel/AMD実機と開発ツールのない別Windows
アカウントの検証を完了させます。検証JSONはGPUベンダー、実行GPU、固定demo、出力寸法、
エンジンSHA-256、証跡SHA-256を自動監査してからリリースゲートへ登録します。
それまでの間は、NVIDIA証跡と同じエンジンをGitHub Actionsで再現できた場合に限り、
Intel／AMD／別アカウント未検証を明記したDevelopment Previewとして配布します。


Mac版で完成した本棚、見開き、次巻移動、原画比較、画質モード、
適応先読みを共通コードとして維持し、Windows 10/11 x64へ移植します。

- PySide6と現在の共通コードを継続利用
- Python、uv、CUDA不要のポータブルZIP
- Real-CUGAN ncnn Vulkan Windows版とモデルを同梱
- Intel、AMD、NVIDIAのVulkan対応GPUを対象
- AI補正が利用できない場合も原画で即時閲覧
- PyInstaller one-folderと`pyside6-deploy` standaloneを実測比較済み
- Windows固有の保存先、D&D、フォルダ表示、プロセス起動を分離
- Windows ARM64、インストーラ、CUDA専用高速化は初版後に評価

開発の引き継ぎと競合防止ルールは
`docs/development/windows-handover.md`および
`docs/development/cross-platform-workflow.md`で管理します。

## 完了済み

- 本棚への複数アーカイブ登録、表紙表示、削除
- 右綴じ見開き、1ページずらし、次巻移動、全画面表示
- Real-CUGANの自動補正と原画比較
- 見開き中央寄せと全画面時のステータス行非表示
- 見開き片側だけ補正される問題の防止
- 現在位置周辺だけを保持する補正・表示キャッシュ
- 原画＋3補正のかんたんモード
- マニュアルモードと名前付きカスタム設定
- 読書速度に応じた前方12〜24ページの適応先読み
- 表示デコードの非同期化と100ms以内の原画フォールバック
