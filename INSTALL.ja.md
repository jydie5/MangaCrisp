# MangaCrisp インストールガイド

[English](INSTALL.md) | [日本語](INSTALL.ja.md)

## 対応環境

- Apple Silicon搭載Mac（M1、M2、M3、M4、A18 Pro搭載MacBook Neo）
- macOS 13以降を推奨
- アプリ本体とAIエンジン用に約300MB
- 漫画を展開するため、元アーカイブと同程度以上の空き容量

Intel Macは現在のstandalone版の対象外です。

MacBook Neoについては、MangaCrisp本体と同梱Real-CUGANエンジンがともに
`arm64`ネイティブで、エンジンがMetalを利用するため、構成上は動作する
見込みです。ただし実機では未検証です。8GBユニファイドメモリと小規模な
GPUのため、MシリーズのPro搭載MacよりAI先読みが遅くなる可能性があります。
詳細は[ハードウェア互換性](docs/hardware-compatibility.md)を参照してください。

## ダウンロード

1. [MangaCrisp Releases](https://github.com/jydie5/MangaCrisp/releases)を開きます。
2. 最新リリースのAssetsを開きます。
3. ファイル名に`standalone.zip`を含むものをダウンロードします。
4. `.sha256`はダウンロードが壊れていないか確認するためのチェックサムです。

`Source code (zip)`は一般ユーザー向けアプリではありません。

## インストール

1. ダウンロードしたZIPをダブルクリックします。
2. 展開された`MangaCrisp.app`をFinderの`アプリケーション`へドラッグします。
3. `アプリケーション`にある`MangaCrisp.app`をControlキーを押しながらクリックします。
4. メニューから`開く`を選びます。
5. 確認画面でも`開く`を選びます。

2回目以降は通常のダブルクリックで起動できます。

## 表示言語を選ぶ

MangaCrispはmacOSの優先言語に従い、英語と日本語を収録しています。本棚右上の
`言語`でシステム設定、英語、日本語を選択し、MangaCrispを再起動してください。

## 「開発元を確認できない」と表示される場合

現在のβ版はApple Developer ID署名を行っていません。配布元がこのGitHubリポジトリであることを確認してから、次の操作を行ってください。

1. 一度`MangaCrisp.app`を通常どおり開き、警告を閉じます。
2. `システム設定`を開きます。
3. `プライバシーとセキュリティ`を開きます。
4. MangaCrispに関する表示の`このまま開く`を押します。

それでも起動できない場合は、ダウンロードしたファイル名、macOSのバージョン、表示された警告文をGitHub Issuesへ報告してください。ターミナルでセキュリティ機能を無効化する必要はありません。

## 本を追加する

本棚へ次のいずれかをドラッグ＆ドロップします。

- ZIP / CBZ
- RAR / CBR
- 7z / CB7
- 画像が入ったフォルダ
- 単独の画像

確認画面で登録すると、MangaCrispが`~/MangaCrisp Library`へ読書用コピーを作り、
表紙を本棚へ追加します。元ファイルは削除しません。旧RAIV for macからの初回起動
では、旧既定の`~/RAIV Library`をコピーせず新名称へ変更します。ユーザーが選択した
カスタム保存先は変更しません。

## キャプチャ用の画面収録を許可する

連番スクリーンキャプチャはmacOS版のベータ機能です。自分が権利を持つ画面、または
保存を許可された画面だけに使用してください。自動ページ送り、画面保護の回避、
撮影画像の外部送信は行いません。

初回だけmacOSの画面収録許可が必要です。

1. 本棚の`画面を連番キャプチャ`を開きます。
2. `範囲を選択`で撮影対象を囲み、`撮影を開始`を押します。
3. 確認が表示されたら、`システム設定 > プライバシーとセキュリティ >
   画面とシステムオーディオ録音`を開きます。macOSの版によっては`画面収録`です。
4. MangaCrispを有効にします。`終了して再度開く`を押してもアプリが残る場合は、
   MangaCrispを自分で完全終了し、同じ`アプリケーション`内のアプリを開き直します。
5. 再起動後に画面内容へのアクセス確認がもう一度表示されたら`許可`を押します。
   この2段目の確認後は通常、再起動不要です。
6. Capture画面が`許可済み（撮影できます）`になったことを確認します。

配布アプリとソースから起動した開発版は、macOS上で別のアプリとして扱われます。
また、ad-hoc署名の開発版は再ビルド後に再許可が必要になる場合があります。
`アプリケーション`へ置いた同じMangaCrispを許可し、そのまま使用してください。

### 撮影して本を完成する

1. セッション名、保存先、ディスプレイ、固定の撮影範囲を設定します。
2. `撮影を開始`を押します。本棚とCapture画面が隠れ、対象アプリの操作を邪魔しません。
3. 画像ごとに`Option+C`を1回押します。直前取消は`Option+Z`です。成功した画像は
   その都度、カラーの連番PNGとして即時保存されます。
4. DockのMangaCrispをクリックするとCapture画面だけが戻ります。本棚は
   `本棚へ戻る`を選ぶまで前面へ出ません。
5. `撮影を完了`を押します。撮影を停止し、保存待ちPNGを確定してからCBZ／ZIPを
   1個作ります。同じページ構成を連打で複数回完成することはできません。

セッションフォルダには完成アーカイブとともに`pages/000001.png`、`000002.png`…が
残ります。1枚の撮影画像に見開き全体が含まれる場合は、完成した本を開き、`V`キーで
`単ページ（1画像）`へ切り替えてください。

詳細な確認手順は
[docs/testing/capture-human-check.ja.md](docs/testing/capture-human-check.ja.md)にあります。

## AI補正を確認する

1. 本を開きます。
2. `P`キーで読書設定を表示します。
3. 状態が`補正済み`になるまで待ちます。
4. `原画を表示（OFFで補正版）`をON/OFFします。

ONが原画、OFFが補正版です。高解像度ページや補正待ちページでは見た目が変わらない場合があり、その理由は状態欄へ表示されます。

## 更新

新しいバージョンのstandalone ZIPをダウンロードし、新しい`MangaCrisp.app`で古いアプリを置き換えます。本棚データと読書位置はアプリ外へ保存されるため、通常はそのまま引き継がれます。

## 問題を報告する

[GitHub Issues](https://github.com/jydie5/MangaCrisp/issues)に次を記載してください。

- Macのモデル（例: M4 Pro MacBook Pro）
- macOSのバージョン
- MangaCrispのバージョン
- 使用したファイル形式（内容や著作物そのものは添付不要）
- 再現手順
- 表示されたエラー文

著作権のある漫画ファイルやページ画像はIssueへ添付しないでください。
